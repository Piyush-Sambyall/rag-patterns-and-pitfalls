# Copy this file to .env and fill in your key.
# Get a key from https://console.anthropic.com/settings/keys
# If you leave this blank, the pipeline still runs, but falls back to
# an extractive (non-LLM) generator instead of calling Claude.
ANTHROPIC_API_KEY=
